# 运行时生成
comment.db
comment.db-wal
comment.db-shm
comment-e2e.db
comment-e2e.db-wal
comment-e2e.db-shm

# 压测产物（stress-write.py 生成，含测试数据）
stress.db
stress.db-wal
stress.db-shm

# e2e 运行时产物
.e2e/*.json

# 数据库备份文件
*.bak
*.bak-*
*.db.pre-acceptance.bak

# 个人博客文章（不属于代码仓库，勿入库）
blog-*.md

# 迁移调试数据目录（含 .env 密钥 / sqlite 用户数据，禁止入库）
waline_*/

# 环境变量文件（可能含 SMTP/密钥等凭据）
.env

# 本地配置文件（含敏感密钥，不入库，按需分发）
config/config.yaml
config/config.local.yaml
/config.yaml

# 生产实例配置（含 JWT secret / SMTP 授权码等凭据，不入库）
config-denjuy.yaml
cmt.denjuy.com.conf
dwx-comment.conf
dwx-comment-denjuy.service

# 部署凭据（SSH 私钥等，不入库）
.deploy/

# 运行时缓存（头像磁盘缓存等）
data/

# 编译产物
dwx-comment
dwx-comment.exe
dwx-comment-test.exe
dwx-comment-linux
dwxcmt-linux
light-comment
light-comment.exe
light-comment-linux

# 前端构建
node_modules/
dist/
front/comment.min.js
front/comment.min.css

# 系统文件
.DS_Store
Thumbs.db

# Python 缓存
__pycache__/
*.pyc
